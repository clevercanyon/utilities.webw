/**
 * Test suite.
 */
// @ts-nocheck -- sandbox.

import { describe, test, expect } from 'vitest';

describe('sandbox: index', async () => {
	test('expect', async () => {
		expect(true).toBe(true);
	});
});
